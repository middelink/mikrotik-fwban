language: go

go:
  - 1.11.x
  - master

branches:
  only:
    - master

before_install:
  - go get github.com/mattn/goveralls

install:
  - go version
  - go env

script:
  - go vet -v .
  - $HOME/gopath/bin/goveralls -service=travis-ci
