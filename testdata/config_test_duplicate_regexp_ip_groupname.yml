in: |-
        [settings]

        [regexps]
         re = "Dummy regexp for (?P<IP>\\S+)|(?P<IP>\\s+)"

        [Mikrotik "MT-1"]
          address = 1.2.3.4
          user = user
          passwd = passwd

err:
        - 'multiple named groups `IP` in regexp "Dummy regexp for (?P<IP>\\S+)|(?P<IP>\\s+)"'
