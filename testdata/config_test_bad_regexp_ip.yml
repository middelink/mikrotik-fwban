in: |-
        [settings]

        [regexps]
         re = "Dummy regexp for \\[(?P<IP>[a-f0-9:]+|[0-9.]+)\\]"
         test-re = "Dummy regexp for [1.2.3.999]"

        [Mikrotik "MT-1"]
          address = 1.2.3.4
          user = user
          passwd = passwd

err:
        - 'unable to parse IP from test-re "1.2.3.999"'
